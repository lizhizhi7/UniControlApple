//
//  UniControlCore.swift
//  UniControlCore
//
//  Public API exports for UniControl library
//

import Foundation
import ApplicationServices

// Re-export all public types and functions for library consumers

// MARK: - DSL Types
// ElementSelector, Action, Command, ExecutionMode, CommandResult, VisionElement, ExecutionContext
// are already public in DSLTypes.swift

// MARK: - DSL Parser & Executor
// DSLParser and DSLExecutor are already public

// MARK: - Server Types
// All server types are already public

// MARK: - Core Functions
// All core functions (AppLauncher, ElementFinder, ElementInteraction) are already public

// MARK: - Utilities
// Permissions, ElementDebugger, SuggestionEngine are already public

// MARK: - Extensions
// DSL extensions are already public
